int GetNth(struct node* head, int index){
  // Code here
  struct node* temp = head;
  int count = 0;
  while(temp!=NULL)
  {
      if(count == index)
      {
          return temp->data;
      }
      count++;
      temp = temp->next;
      
  }
}
